import { useState } from "react"; import { Lock,Sparkles } from "lucide-react"; import { useAuth } from "../context/AuthContext"; export function Login(){const {login}=useAuth();const [username,setUsername]=useState("admin");const [password,setPassword]=useState("123456");const [loading,setLoading]=useState(false);const [error,setError]=useState("");async function submit(e:React.FormEvent){e.preventDefault();try{setLoading(true);setError("");await login(username,password)}catch{setError("Credenciales incorrectas o API no disponible.")}finally{setLoading(false)}} return <div className="grid min-h-screen grid-cols-1 bg-slate-950 lg:grid-cols-2"><div className="flex items-center justify-center p-6"><div className="w-full max-w-md rounded-[2rem] bg-white p-8 shadow-2xl"><div className="mb-8"><div className="mb-4 inline-flex h-12 w-12 items-center justify-center rounded-2xl bg-emerald-100 text-emerald-700"><Lock/></div><h1 className="text-3xl font-black text-slate-950">Iniciar sesión</h1><p className="mt-2 text-sm text-slate-500">Accede al dashboard conectado a tu API de facturación .NET.</p></div><form onSubmit={submit} className="space-y-4"><div><label className="mb-2 block text-sm font-bold text-slate-700">Usuario</label><input className="input" value={username} onChange={e=>setUsername(e.target.value)}/></div><div><label className="mb-2 block text-sm font-bold text-slate-700">Contraseña</label><input className="input" type="password" value={password} onChange={e=>setPassword(e.target.value)}/></div>{error&&<p className="rounded-xl bg-red-50 p-3 text-sm font-bold text-red-600">{error}</p>}<button disabled={loading} className="btn-primary w-full">{loading?"Validando...":"Entrar al sistema"}</button></form><div className="mt-6 rounded-2xl bg-slate-50 p-4 text-sm text-slate-500">Demo: <b>admin</b> / <b>123456</b></div></div></div><div className="hidden items-center justify-center bg-[radial-gradient(circle_at_top,#10b981,transparent_40%),linear-gradient(135deg,#020617,#0f172a)] p-10 lg:flex"><div className="max-w-lg text-white"><div className="mb-6 inline-flex items-center gap-2 rounded-full bg-white/10 px-4 py-2 text-sm font-bold text-emerald-200"><Sparkles size={16}/>Proyecto frontend para portafolio</div><h2 className="text-5xl font-black leading-tight">Dashboard moderno para una API empresarial.</h2><p className="mt-6 text-lg leading-8 text-slate-300">Login con JWT, consumo de endpoints protegidos, CRUD visual, emisión de facturas y métricas de ventas.</p></div></div></div>}
