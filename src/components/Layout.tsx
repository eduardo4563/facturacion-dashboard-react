import { BarChart3,FileText,LogOut,Package,Users } from "lucide-react"; import { useAuth } from "../context/AuthContext"; type Tab="dashboard"|"clientes"|"productos"|"facturas"; export function Layout({tab,setTab,children}:{tab:Tab;setTab:(tab:Tab)=>void;children:React.ReactNode}){const {logout}=useAuth(); const nav=[{id:"dashboard" as const,label:"Dashboard",icon:BarChart3},{id:"clientes" as const,label:"Clientes",icon:Users},{id:"productos" as const,label:"Productos",icon:Package},{id:"facturas" as const,label:"Facturas",icon:FileText}]; return <div className="min-h-screen bg-slate-50"><aside className="fixed left-0 top-0 hidden h-screen w-72 border-r border-slate-200 bg-white p-5 lg:block"><div className="mb-8"><div className="rounded-3xl bg-slate-950 p-5 text-white"><p className="text-xs font-bold uppercase tracking-[0.2em] text-emerald-300">Facturación</p><h1 className="mt-2 text-2xl font-black leading-tight">Dashboard Empresarial</h1><p className="mt-2 text-sm text-slate-300">React + .NET API + JWT</p></div></div><nav className="space-y-2">{nav.map((item)=>{const Icon=item.icon;const active=tab===item.id;return <button key={item.id} onClick={()=>setTab(item.id)} className={`flex w-full items-center gap-3 rounded-2xl px-4 py-3 text-sm font-bold transition ${active?"bg-emerald-600 text-white":"text-slate-600 hover:bg-slate-100"}`}><Icon size={18}/>{item.label}</button>})}</nav><button onClick={logout} className="absolute bottom-5 left-5 right-5 flex items-center justify-center gap-2 rounded-2xl border border-slate-200 px-4 py-3 text-sm font-bold text-slate-600 transition hover:bg-slate-100"><LogOut size={18}/>Cerrar sesión</button></aside><main className="lg:pl-72"><header className="sticky top-0 z-10 border-b border-slate-200 bg-white/80 px-5 py-4 backdrop-blur lg:px-8"><div className="flex items-center justify-between gap-4"><div><p className="badge">API conectada</p><h2 className="mt-2 text-xl font-black text-slate-950 lg:text-2xl">Sistema de Facturación</h2></div><button onClick={logout} className="btn-secondary lg:hidden"><LogOut size={18}/></button></div><div className="mt-4 flex gap-2 overflow-x-auto lg:hidden">{nav.map((item)=>{const Icon=item.icon;const active=tab===item.id;return <button key={item.id} onClick={()=>setTab(item.id)} className={`flex shrink-0 items-center gap-2 rounded-xl px-3 py-2 text-xs font-bold ${active?"bg-emerald-600 text-white":"bg-slate-100 text-slate-600"}`}><Icon size={15}/>{item.label}</button>})}</div></header><div className="p-5 lg:p-8">{children}</div></main></div>}
